import styled from 'styled-components'

const PersonInfoWrapper = styled.div``

const LoginModalWrapper = styled.div`
  .ant-modal-footer {
    text-align: center;
  }
`

export { LoginModalWrapper, PersonInfoWrapper }
